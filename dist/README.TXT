To run the project from the command line, go to the dist folder and
type the following:

java -jar "OpenCVFotograf.jar" 
----------------------------------------------------------------
Projeyi komut satırından çalıştırmak için dist klasörüne gidin ve
aşağıdaki komutu çalıştırın:

java -jar "OpenCVFotograf.jar" 
